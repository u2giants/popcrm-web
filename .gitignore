# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# Supabase CLI local state (this repo has no Supabase config/migrations of its
# own — they live in u2giants/shared-db; only `gen types` is run here, which
# leaves a transient supabase/.temp/).
supabase/

# Playwright MCP / screenshot session artifacts
.playwright-mcp/
screenshot-*.png
