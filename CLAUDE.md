# CLAUDE.md — popcrm-web

Read `AGENTS.md` first.

This is the POP CRM frontend, not the PIM frontend. The correct repo/container/package name is `popcrm-web`.

For the frontend beautification plan, read `frontend_imp.md`.
